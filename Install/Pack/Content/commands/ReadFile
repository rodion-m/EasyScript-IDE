<h3>Описание</h3>
Команда предназначена для считывания содержимого файла в переменную.
<h3>Синтаксис</h3>
<code>Считать файл(имя файла, переменная для получения содержимого[], начиная с какой строки [1], количество строк [все])</code>
Если переменная не существует, то она автоматически инициализируется.
<h3>Примеры</h3><code>
' Следующая команда считает содержимое файла "C:\file" в переменную X:
Считать файл("C:\file", X)
</code>
<hr />
En: ReadFile
ID: 1013